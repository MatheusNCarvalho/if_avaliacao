<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             BackgroundColor="{StaticResource BackgroundColor}"
             x:Class="IFAvaliacao.Views.PreenchimentoPage">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Style x:Key="FrameOptions" TargetType="Frame">
                <Setter Property="Margin" Value="10" />
            </Style>

            <Style x:Key="StackLayoutOptions" TargetType="StackLayout">
                <Setter Property="Padding" Value="0"/>
                <Setter Property="Spacing" Value="0"/>
            </Style>
            <Style TargetType="Label">
                <Setter Property="TextColor" Value="Black"/>
            </Style>
            <Style x:Key="LabelTitulo" TargetType="Label">
                <Setter Property="TextColor" Value="Black"/>
                <Setter Property="TextDecorations" Value="Underline"/>
                <Setter Property="FontSize" Value="Medium"/>
               
            </Style>
            <Style x:Key="LabelItemEnd" TargetType="Label">
                <Setter Property="TextColor" Value="Black"/>
                <Setter Property="HorizontalTextAlignment" Value="End"/>
                <Setter Property="LineBreakMode" Value="TailTruncation"/>
            </Style>
            <Style x:Key="LabelItemCenter" TargetType="Label">
                <Setter Property="HorizontalTextAlignment" Value="Center"/>
                <Setter Property="TextColor" Value="Black"/>
                <Setter Property="FontAttributes" Value="Bold"/>
            </Style>
            <Style x:Key="LabelSliderValue" TargetType="Label">
                <Setter Property="FontSize" Value="Medium"/>
                <Setter Property="TextColor" Value="DarkGreen"/>
            </Style>
            <Style TargetType="Slider">
                <Setter Property="Margin" Value="-10"/>
                <Setter Property="MaximumTrackColor" Value="OrangeRed"/>
                <Setter Property="MinimumTrackColor" Value="Black"/>
            </Style>

            <Style x:Key="FlexLayoutRows" TargetType="FlexLayout">
                <Setter Property="Padding" Value="0"/>
                <Setter Property="AlignItems" Value="Stretch"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.Content>
        <StackLayout 
            Spacing="10"
            Padding="5">

            <Label 
                FontSize="Medium"
                Text="{Binding NameCow, StringFormat='Nome da vaca: {0}'}" />

            <Label FontSize="Medium" 
                   Text="{Binding BodyWight, StringFormat='Peso corporal da balança: {0}'}"/>

            <ScrollView Orientation="Vertical">
               <Grid RowSpacing="15">
                   <Grid.RowDefinitions>
                       <RowDefinition Height="*" />
                       <RowDefinition Height="*" />
                   </Grid.RowDefinitions>

                    <StackLayout Grid.Row="0" Style="{StaticResource StackLayoutOptions}">
                        <Label Text="Escolha há Angulosidade" Style="{StaticResource LabelTitulo}"/>

                        <FlexLayout  Direction="Row" Style="{StaticResource FlexLayoutRows}">
                            <Slider FlexLayout.Grow="1" Value="{Binding Angulosiodade}" Minimum="{Binding SliderMinimum}" Maximum="9"/>

                            <Label Style="{StaticResource LabelSliderValue}" Text="{Binding Angulosiodade, StringFormat='{0:0}'}"/>
                        </FlexLayout>

                        <FlexLayout Direction="Row" Style="{StaticResource FlexLayoutRows}">
                            <Label FlexLayout.Grow="1" Text="Sem angulosidade">
                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding TapCommand}" CommandParameter="Angulosiodade.1,08"/>
                                </Label.GestureRecognizers>
                            </Label>

                            <Label FlexLayout.Grow="1" Text="Intermediário" Style="{StaticResource LabelItemCenter}">
                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding TapCommand}" CommandParameter="Angulosiodade.5,08"/>
                                </Label.GestureRecognizers>
                            </Label>

                            <Label FlexLayout.Grow="1" Style="{StaticResource LabelItemEnd}" Text="Angulosa e descamada">
                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding TapCommand}" CommandParameter="Angulosiodade.9,08"/>
                                </Label.GestureRecognizers>
                            </Label>
                        </FlexLayout>
                    </StackLayout>

                   <StackLayout Grid.Row="1" Style="{StaticResource StackLayoutOptions}">
                       <Label Text="Escolha há Angulosidade" Style="{StaticResource LabelTitulo}"/>

                       <FlexLayout  Direction="Row" Style="{StaticResource FlexLayoutRows}">
                           <Slider FlexLayout.Grow="1" Value="{Binding Angulosiodade}" Minimum="{Binding SliderMinimum}" Maximum="9"/>

                           <Label Style="{StaticResource LabelSliderValue}" Text="{Binding Angulosiodade, StringFormat='{0:0}'}"/>
                       </FlexLayout>

                       <FlexLayout Direction="Row" Style="{StaticResource FlexLayoutRows}">
                           <Label FlexLayout.Grow="1" Text="Sem angulosidade">
                               <Label.GestureRecognizers>
                                   <TapGestureRecognizer Command="{Binding TapCommand}" CommandParameter="Angulosiodade.1,08"/>
                               </Label.GestureRecognizers>
                           </Label>

                           <Label FlexLayout.Grow="1" Text="Intermediário" Style="{StaticResource LabelItemCenter}">
                               <Label.GestureRecognizers>
                                   <TapGestureRecognizer Command="{Binding TapCommand}" CommandParameter="Angulosiodade.5,08"/>
                               </Label.GestureRecognizers>
                           </Label>

                           <Label FlexLayout.Grow="1" Style="{StaticResource LabelItemEnd}" Text="Angulosa e descamada">
                               <Label.GestureRecognizers>
                                   <TapGestureRecognizer Command="{Binding TapCommand}" CommandParameter="Angulosiodade.9,08"/>
                               </Label.GestureRecognizers>
                           </Label>
                       </FlexLayout>
                   </StackLayout>

                   <StackLayout Grid.Row="2" Style="{StaticResource StackLayoutOptions}">
                       <Label Text="Escolha há Angulosidade" Style="{StaticResource LabelTitulo}"/>

                       <FlexLayout  Direction="Row" Style="{StaticResource FlexLayoutRows}">
                           <Slider FlexLayout.Grow="1" Value="{Binding Angulosiodade}" Minimum="{Binding SliderMinimum}" Maximum="9"/>

                           <Label Style="{StaticResource LabelSliderValue}" Text="{Binding Angulosiodade, StringFormat='{0:0}'}"/>
                       </FlexLayout>

                       <FlexLayout Direction="Row" Style="{StaticResource FlexLayoutRows}">
                           <Label FlexLayout.Grow="1" Text="Sem angulosidade">
                               <Label.GestureRecognizers>
                                   <TapGestureRecognizer Command="{Binding TapCommand}" CommandParameter="Angulosiodade.1,08"/>
                               </Label.GestureRecognizers>
                           </Label>

                           <Label FlexLayout.Grow="1" Text="Intermediário" Style="{StaticResource LabelItemCenter}">
                               <Label.GestureRecognizers>
                                   <TapGestureRecognizer Command="{Binding TapCommand}" CommandParameter="Angulosiodade.5,08"/>
                               </Label.GestureRecognizers>
                           </Label>

                           <Label FlexLayout.Grow="1" Style="{StaticResource LabelItemEnd}" Text="Angulosa e descamada">
                               <Label.GestureRecognizers>
                                   <TapGestureRecognizer Command="{Binding TapCommand}" CommandParameter="Angulosiodade.9,08"/>
                               </Label.GestureRecognizers>
                           </Label>
                       </FlexLayout>
                   </StackLayout>
                </Grid>
            </ScrollView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>